Somwhere to test Deckset's settings.
These directives go at the very top of the file.

- footer
    - set the footer for all slides 
    - e.g. `footer: © Unsigned Integer UG, 2014`

- slidenumbers: use slide numbers
    - e.g. `slidenumbers: true`

- autoscale
    - EXPERIMENTAL
    - automatically scales down long paragraphs so they fit on the slide (rather than being cropped)
    - via https://twitter.com/decksetapp/status/580483793808658432
    - e.g. `autoscale: true`
